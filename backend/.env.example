# Backend ML Automator — environment template
# Copy to backend/.env and fill in your key (backend/.env is gitignored).

# Groq API key — used by the per-stage refinement agent (llm_agent.py).
# Get one at https://console.groq.com/keys
GROQ_API_KEY=

# Model used for refinement recommendations (Groq).
GROQ_MODEL=openai/gpt-oss-120b

# -- Optional alternative providers (all OpenAI-compatible) -----------------
# Any provider in the catalog (backend/ai_providers.py) can be activated either
# from the settings UI (write-only key) or by setting its env var here. At call
# time resolve() uses the per-backend stored key first, else the provider env var.
#   OPENAI_API_KEY=
#   MISTRAL_API_KEY=
#   DEEPSEEK_API_KEY=
#
# Z.AI / Zhipu GLM family -- models: glm-5.2 (recommended), glm-5.1, glm-5,
# glm-4.7, glm-4.6, glm-4.5. Endpoint https://api.z.ai/api/paas/v4
# Docs: https://docs.z.ai/api-reference/introduction
#   ZAI_API_KEY=

# Session persistence (SQLite). Sessions are mirrored to disk and survive a
# backend restart. Set ML_PERSIST_SESSIONS=0 to run purely in-memory.
# ML_PERSIST_SESSIONS=1
# ML_SESSION_DB=backend/sessions.db
